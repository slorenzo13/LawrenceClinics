<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lawrenceclinics">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AgendarCita"></activity>
        <activity android:name=".VerificarCita" />
        <activity android:name=".Calendario" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HorariosDisponibles" />
        <activity android:name=".AgregarCita" />
        <activity android:name=".AgendaCitasMedicas" />
        <activity android:name=".registro_perfil" />
    </application>

</manifest>